eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

server:
  port: 8080

spring:
  application:
    name: api-gateway
  main:
    web-application-type: reactive
  cloud:
    gateway:

      ## Services Routes

      routes:
        - id: demo-service
          uri: lb://demo-service
          predicates:
            - Path=/demo

        - id: account-service
          uri: lb://account-service
          predicates:
            - Path=/test

        - id: security-service
          uri: lb://security-service
          predicates:
            - Path=/api/v1/username, /api/v1/auth/register, /api/v1/auth/authenticate

        ## Discovery Server Route

        - id: discovery-server
          uri: http://localhost:8761
          predicates:
            - Path=/eureka/web
          filters:
            - SetPath=/
        - id: discovery-server-static
          uri: http://localhost:8761
          predicates:
            - Path=/eureka/**